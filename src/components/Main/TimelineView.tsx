import { Center } from '@chakra-ui/react';
import React from 'react';
import { HEADERHEIGHT, FOOTERHEIGHT } from '../../utils';

const TimelineView = () => {
  return <Center h={`calc(100vh - ${HEADERHEIGHT + FOOTERHEIGHT}px)`}>Timeline View not yet implemented</Center>;
};

export default TimelineView;
